[pytest]
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::starlette.exceptions.StarletteDeprecationWarning
markers =
    unit: fast deterministic unit test without external model inference
    integration: subsystem boundary test that may use controlled test doubles
    real_model_smoke: requires installed model weights and performs genuine inference
